/*Meteor.methods({
	postInsert:function(post) {
		var postId = Problems.insert(post);

		return {
			_id: postId
		};
	},

	solutionInsert:function(solution) {
		return Solutions.insert(solution);
	}
});*/


